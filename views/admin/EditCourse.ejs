<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Course</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src='https://kit.fontawesome.com/a076d05399.js' crossorigin='anonymous'></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:ital,wght@0,200..1000;1,200..1000&display=swap" rel="stylesheet">
    <style>
        *{
            font-family: 'Nunito';
        }

        form{
            padding: 20px;
        }

        .card{
            /* box-shadow: rgba(0, 0, 0, 0.15) 1.95px 1.95px 2.6px; */
            box-shadow: rgba(14, 30, 37, 0.12) 0px 2px 4px 0px, rgba(14, 30, 37, 0.32) 0px 2px 16px 0px;
        }
    </style>
</head>

<body>
    <%- include('./partial/navbar') %>

    <div class="container mt-4">
        <h1 style="font-weight: 600; text-align: center;">Edit Course Details</h1>
        <hr>
        <div style="display: flex; justify-content: center;">
            <div class="container-table" style="width: 70%;">
                <div class="card">
                    <form action="/admin/courses/<%= course.id %>/edit" method="post">
                        <label for="category" class="form-label">Category</label>
                        <select name="CategoryId" id="category" class="form-select" style="border-color: black;">
                            <option value="">-- Select Category --</option>
                            <% categories.forEach((e) => { %>
                             <option value="<%= e.id %>" <%= course.CategoryId === e.id ? 'selected' : '' %>><%= e.name %></option>
                            <% }) %>
                        </select><br>
                        <label for="name" class="form-label">Course Title</label>
                        <input type="text" id="name" name="name" class="form-control" placeholder="Please Enter Category Name" style="border-color: black;" value="<%= course.name %>"><br>
                        <label for="description" class="form-label">Course Description</label>
                        <textarea name="description" id="description" rows="10" class="form-control" style="border-color: black;" ><%= course.description %></textarea><br>
                        <label for="image" class="form-label">Image Url</label>
                        <input type="text" id="image" name="image" class="form-control" placeholder="Please Enter Image Url" style="border-color: black;" value="<%= course.image %>"><br>
                        <label for="jumlahModul" class="form-label">Jumlah Modul</label>
                        <input type="number" id="jumlahModul" name="jumlahModul" class="form-control" placeholder="Please Enter Jumlah Modul" style="border-color: black;" value="<%= course.jumlahModul %>"><br>
                        <button class="btn btn-success">Submit Edit</button>
                    </form>
                </div>
            </div>
        </div>
        <div style="height: 100px;"></div>
    </div>


</body>

</html>